namespace Sol.Actions
{
    /// <summary>
    /// Base for actions that enter or exit a continuous state system (grab, rest, combat).
    /// </summary>
    public abstract class StateAction : GameAction { }
}
